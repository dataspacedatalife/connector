Vault has been deployed!

Release name: {{ .Release.Name }}
Namespace:    {{ .Release.Namespace }}

Service:  {{ .Values.service.name }}:{{ .Values.service.port }}
Root Token:   {{ .Values.rootToken }}

To access Vault from inside the cluster:
  kubectl port-forward svc/{{ .Values.service.name }} 8200:{{ .Values.service.port }} -n {{ .Release.Namespace }}

Then open:
  http://127.0.0.1:8200

Verify pods:
  kubectl get pods -l app={{ .Values.app }} -n {{ .Release.Namespace }}
