PostgreSQL has been deployed!

Release name: {{ .Release.Name }}
Namespace:    {{ .Release.Namespace }}

Service:   {{ .Values.service.name }}:{{ .Values.service.port }}
Database:  {{ .Values.global.postgres.db.name }}
User:      {{ .Values.global.postgres.db.user }}
Password:  {{ .Values.global.postgres.db.password }}

To connect from inside the cluster:
  kubectl run psql-client --rm -it \
    --image=postgres:{{ .Values.image.tag }} \
    --namespace {{ .Release.Namespace }} \
    -- psql -h {{ .Values.service.name }} -U {{ .Values.global.postgres.db.user }} -d {{ .Values.global.postgres.db.name }}
