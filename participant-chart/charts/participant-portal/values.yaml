deployment:
  replicaCount: 1
  imagePullSecret: harbor-regcred-usuarios
  image:
    repository: harbor.gradiant.org/si-xdatashare-usuarios-pr-01616/participant-portal
    tag: "dev"
    pullPolicy: Always
service:
  type: ClusterIP
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 80
