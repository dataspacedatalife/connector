deployment:
  replicaCount: 1
  image:
    repository: dataspacedatalife/participant-portal
    tag: "latest"
    pullPolicy: Always

service:
  type: ClusterIP
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 80